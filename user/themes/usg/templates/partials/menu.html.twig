<nav class="navbar" role="navigation">       
    <div class="container">
      <div class="home">
        <a href="{{ base_url == '' ? '/' : base_url }}" title="home">
          <div class="toit">
            <div class="toit1">
             </div>
             <div class="toit2">
             </div>
          </div>
          <div class="mur"></div>
        </a>
      </div>
      <div class="menu-toggle">
        <div></div>
        <div></div>
      </div>
      <div class="navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          {% for page in pages.children.visible %}
            {% set current_page = (page.active or page.activeChild) ? 'active' : '' %}
            <li class="{{ page.title }}">
              <a href="{{ page.url }}">{{ page.menu }}</a>
              {% if page.children is not empty %}
                  <ul class="submenu">
                    <div class="carreblanc"></div>
                  {% for child in page.children %}
                        <li class="{{ current_page }} {{child.title |replace({' ': '',}) |replace({'&': '',}) |replace({'&': '',})}}"><a href="#" data-title="{{child.title |replace({' ': '',}) |replace({'&': '',}) |replace({'+': '',})}}">{{ child.menu }}</a></li>
                  {% endfor %}
                  </ul>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
      </div>
    </div>
</nav>